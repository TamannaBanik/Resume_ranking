[project]
name = "resume-ranking-reranked"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lancedb>=0.33.0",
    "pandas>=3.0.3",
]
